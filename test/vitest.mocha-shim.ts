import { afterAll, afterEach, beforeAll, beforeEach, describe, it, test } from "vitest";

export {
  afterAll as after,
  afterEach as teardown,
  beforeAll as before,
  beforeEach as setup,
  describe,
  describe as context,
  describe as suite,
  it,
  test
};
